library(shiny)
library(leaflet)
library(rgdal)
library(sp)

ui <- fluidPage(
  # Assign a title to your application
  titlePanel("Munich in Figures"),
  sidebarLayout(
    sidebarPanel(
    ),
    mainPanel(
      
    )
  )
)
